# Copy to .env and fill in — never commit the real file.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
DEEPSEEK_API_KEY=

# Optional: override the contact email / repo URL that appears in the User-Agent
# header on every PyPI / npm registry request (be a good citizen).
SLOP_BENCH_CONTACT_EMAIL=
SLOP_BENCH_REPO_URL=
